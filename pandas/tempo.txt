Need to explore

df = df.sort_values(
    ["customer", "date"],
    ignore_index=True
)
equivelent to 
df=df.sort_values(['customer','date']).reset_index(drop=True)

why ignore_index here , What is doing ignore_index ?



need example for
   values
        .shift(1)

specially for shift() function