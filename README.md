# first_ml

## Start jupyter nootbook as host mood
**`Disable security , authentication and token also`**
```sh
jupyter notebook --ip=0.0.0.0 --port=8888 --NotebookApp.token='' --NotebookApp.password=''
```